name = "prism-converter"
main = "src/index.js"
compatibility_date = "2024-12-01"

rules = [
  { type = "Text", globs = ["**/*.html"] }
]

[vars]
PROXY_DOMAIN = "your-proxy.example.com"
# SECRET_KEY should be set via `wrangler secret put SECRET_KEY`
